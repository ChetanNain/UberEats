import { render, screen } from '@testing-library/react';
import Card from './Card.js';
import React from 'react';

describe('', ()=>{
    it('Card should render when called', ()=>{
        render(<Card/>);
    })
})
